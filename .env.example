# Local testing only. Copy to `.env` and fill in real values — never commit `.env`.
# On Prefect Cloud, these are read from Prefect Secret blocks of the same name instead.

TAVILY_API_KEY_1=
TAVILY_API_KEY_2=
TAVILY_API_KEY_3=
TAVILY_API_KEY_4=

OPENAI_API_KEY=          # DeepSeek API key (OpenAI-compatible)
GROQ_API_KEY=            # fallback if DeepSeek call fails, used via OpenAI SDK

RESEND_API_KEY=
EMAIL_FROM=onboarding@resend.dev
EMAIL_TO=

GITHUB_TOKEN=            # PAT with repo write access
GITHUB_REPO=exc33ded/prefect-AI-news-run
